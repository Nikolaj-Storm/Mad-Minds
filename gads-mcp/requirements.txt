# Pinned for the deployed Google Ads MCP.
fastmcp>=2.12,<3
google-ads>=24.0.0
python-dotenv>=1.0.0
py-key-value-aio>=0.3.0
diskcache>=5.6.0
# Upstash Redis HTTP client — used by RedisStore for Vercel / stateless deploys.
# Not needed on a Docker/VPS path (CLIENT_STORAGE_DIR), but harmless to install.
upstash-redis>=1.0.0
